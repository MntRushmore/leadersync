{
  "name": "webapp",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "echo 'Vercel handles the build via api/index.tsx'",
    "clean-port": "fuser -k 3000/tcp 2>/dev/null || true",
    "test": "curl http://localhost:3000"
  },
  "dependencies": {
    "hono": "^4.9.8"
  },
  "devDependencies": {
    "@hono/vite-dev-server": "^0.18.2",
    "typescript": "^5.0.0",
    "vite": "^6.3.5"
  }
}
